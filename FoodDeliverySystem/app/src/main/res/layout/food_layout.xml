<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="1">

    <ImageView
        android:id="@+id/food_imageview"
        android:layout_width="match_parent"
        app:srcCompat="@mipmap/ic_launcher"
        android:layout_marginTop="5sp"
        android:layout_height="100sp"
        />

    <TextView
        android:id="@+id/food_name_price_textview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/food_imageview"
        android:layout_marginTop="10sp"
        android:text="TextView"
        android:textSize="18sp" />

    <ProgressBar
        android:id="@+id/food_image_loading_progressbar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/food_name_price_textview"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="30sp"
        android:layout_marginEnd="194sp"
        android:layout_marginRight="100sp" />
</RelativeLayout>